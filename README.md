# ISE-Recorder

This is a fairly simple web-based lecture recorder.

To run, clone repo and run

```
npm install
npm run dev
```

then visit http://localhost:3000

It's hacked together on top of next.js, so check their doc for production deployments. I'll probably containerize this at some point.
